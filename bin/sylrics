#!/usr/bin/env python3
import sys
from pathlib import Path
root=Path(__file__).resolve().parents[1]
sys.path.insert(0,str(root if (root/'cli.py').exists() else Path('/usr/lib/sylrics')))
from cli import main
raise SystemExit(main())
